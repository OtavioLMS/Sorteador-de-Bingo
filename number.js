class Number{
    
    var numero;
    var letra;
    
    function show(){
        let result = "<div class='numero saiu' id='" + letra + '' + numero + "'><p class='letra'>' + letra + '</p><p class='nul'></p>" + numero + "</div>";
        return result;
    }
    
}